# HANDOFF — งบการเงิน (D3 · งบดุล / งบกำไรขาดทุน)

> **P1–P5 เสร็จครบทุกเฟสแล้ว** — JSON + ฟอร์มพิมพ์ + Admin UI (2026-09-19 ถึง 2026-09-20)
> ผลตรวจสอบเต็มอยู่ที่ **§10** (JSON) · **§11** (พิมพ์) · **§12** (Admin UI) ท้ายไฟล์ ·
> ค้างอย่างเดียว: **manual QA หน้าจอ** ซึ่งต้องมีคนคลิกจริง (§12.2) · §5 ทั้งสามข้อได้คำตอบแล้ว (ดู §10.1)
> ส่วน §1–§9 เก็บไว้ตามเดิมเป็นบันทึกการออกแบบ ณ 2026-09-13 — จุดที่ของจริงออกมาต่างจากที่ร่างไว้
> ถูกกำกับไว้ใน §10.2

---

## 0 · สรุปสั้น

งบการเงินเป็นงานที่ **ฐานข้อมูลพร้อมแล้วเกือบทั้งหมด** — nested set (`lft`/`rgt`) ทำให้รวมยอดทั้ง
subtree เป็น query เดียว, `gl_account_year_end_balances` เก็บยอดยกมาถาวรต่อบัญชีต่อปี, และ
`TrialBalancesService` ที่มีอยู่แล้วคือ 80% ของ query ที่งบทั้งสองต้องใช้

ที่ยังไม่มีคือ **การจัดรูป**: งบดุลต้องจัดกลุ่มตาม `account_type` แล้วรวมยอดตามชั้นของผังบัญชี
งบกำไรขาดทุนต้องรู้ว่า "ช่วงเวลา" ของมันคือรอบไหน และทั้งคู่ต้องเทียบปีก่อนหน้าได้ — ซึ่งเป็น
**การตัดสินใจเชิงบัญชี ไม่ใช่เชิงโค้ด** และเป็นเหตุผลเดียวที่ไฟล์นี้เป็น backlog แทนที่จะทำไปเลย

---

## 1 · สถานะโค้ดจริงวันนี้ (ตรวจแล้ว 2026-09-13)

### 1.1 มีอะไรแล้ว

| ของที่มี | อยู่ที่ไหน | ใช้ทำอะไรในงานนี้ |
|---|---|---|
| `gl_accounts` nested set | `erp_finance` · `lft`/`rgt`/`parent_gl_account_id`/`is_group` | รวมยอด subtree ด้วย `BETWEEN lft AND rgt` — ไม่ต้อง recursive CTE |
| `account_type` 5 ค่า | `GlAccountType` (ASSET/LIABILITY/EQUITY/REVENUE/EXPENSE) | แยกงบดุล (3 ตัวแรก) ออกจากงบกำไรขาดทุน (2 ตัวหลัง) |
| `TrialBalancesService.build()` | `apps/finance-bc/src/modules/general-ledger/` | query ต้นแบบ: `SUM(debit)`/`SUM(credit)` ต่อ `account_id` + กรอง `entry_date` ช่วงใดก็ได้ |
| `gl_account_year_end_balances` | `erp_finance` | `opening_balance` / `period_activity` / `closing_balance` ต่อบัญชีต่อปี **แช่แข็งถาวร** |
| `finance_settings.fiscal_year_start_month` | `erp_finance` (ค่าปัจจุบัน = `1`) | ปีบัญชีตั้งค่าได้ต่อ deployment |
| `resolveFiscalYear()` / `fiscalYearBounds()` | `@lib/common` | ถูกวางไว้ใน shared ตั้งแต่ D2 **เพราะรู้อยู่แล้วว่างบการเงินจะต้องใช้** |
| print engine ครบวงจร | report-bc · 25 เอกสาร + ภ.พ.30 | พิมพ์งบได้ทันทีถ้าตัดสินใจว่าจะพิมพ์ |
| `company_profiles` | `erp_finance` | หัวกระดาษงบ (ชื่อกิจการ/เลขผู้เสียภาษี/ที่อยู่) |

**ผังบัญชีจริงบนคลัสเตอร์วันนี้** — 217 บัญชี: ASSET 59 (group 14) · LIABILITY 29 (8) · EQUITY 5 (1) ·
REVENUE 18 (3) · EXPENSE 106 (15) · บัญชีที่เป็น `is_group = true` คือหัวข้อรวม ไม่รับ posting

### 1.2 ยังไม่มีอะไร

- **ไม่มี endpoint งบการเงินเลยสักตัว** — `/trial-balances` เป็นงบทดลอง (รายบัญชี ไม่จัดกลุ่ม)
- **ไม่มี manual journal entry** — ปรับปรุงรายการเพื่อปิดงบไม่ได้ นอกจากออกเอกสารจริง
  (`HANDOFF-Fiscal-Year-Closing.md` §9 — เป็นข้อจำกัดที่ต้องบอกผู้ใช้ ไม่ใช่ช่องว่างของงานนี้)
- **ยังไม่เคยปิดปีบัญชีจริงสักปี** — `fiscal_year_closes` มี **0 แถว** และ `ledger_entries` มี 133 แถว
  กระจุกที่ ASSET/LIABILITY/EXPENSE เท่านั้น (REVENUE ยังไม่มีรายการเลย) → **งบที่ออกมาวันนี้จะ
  แทบว่างเปล่า และ "ยอดยกมา" จะเป็นศูนย์ทุกบัญชีเพราะไม่มีปีก่อนหน้าให้ยกมา** · ข้อนี้กระทบ
  วิธีทดสอบมากกว่ากระทบดีไซน์ (ดู §7)

---

## 2 · ขอบเขต

**อยู่ในรอบนี้**

1. **งบแสดงฐานะการเงิน (งบดุล)** — สินทรัพย์ / หนี้สิน / ส่วนของผู้ถือหุ้น ณ วันที่หนึ่ง
2. **งบกำไรขาดทุน** — รายได้ / ค่าใช้จ่าย ของช่วงเวลาหนึ่ง
3. เทียบงวดก่อนหน้าในคอลัมน์ที่สอง (ดู §5 ข้อ 2 — ต้องยืนยันก่อน)
4. พิมพ์เป็น PDF ผ่าน print engine เดิม (ดู §5 ข้อ 3)

**ไม่อยู่ในรอบนี้** (ดู §8 สำหรับเหตุผล) — งบกระแสเงินสด · หมายเหตุประกอบงบ · งบแสดงการเปลี่ยนแปลง
ส่วนของผู้ถือหุ้น · การรวมงบหลายบริษัท · เปรียบเทียบกับงบประมาณ

---

## 3 · ดีไซน์ที่เสนอ

### 3.1 หลักการเดียวที่ทั้งงานนี้ยืนอยู่บนมัน

> **งบการเงินคือ `ledger_entries` ที่จัดกลุ่มตามผังบัญชี ไม่ใช่ตัวเลขชุดใหม่**

ห้ามสร้างตารางสรุปของตัวเอง ห้ามคำนวณจากเอกสารต้นทาง (receipts/ap_invoices) — ทุกตัวเลขต้อง
สืบย้อนไปถึงแถวใน `ledger_entries` ได้เสมอ · ถ้างบไม่ตรงกับงบทดลอง แปลว่างบผิด ไม่ใช่ ledger ผิด
เหตุผลเดียวกับที่ ภ.พ.30 ไม่เก็บสถานะของตัวเอง

### 3.2 รวมยอดตาม subtree — จุดที่ nested set คุ้มค่าที่สุด

ยอดของบัญชีหัวข้อ (`is_group = true`) = ผลรวมของทุกใบใต้มัน:

```sql
SELECT g.id, g.code, g.name_th,
       SUM(le.debit - le.credit) AS balance
FROM gl_accounts g
JOIN gl_accounts child ON child.lft BETWEEN g.lft AND g.rgt
JOIN ledger_entries le ON le.account_id = child.id
WHERE g.deleted_at IS NULL AND le.entry_date <= :as_of
GROUP BY g.id, g.code, g.name_th;
```

**กับดักที่ต้องระวัง** — `deleted_at IS NULL` ลงไปที่ตารางที่ join ด้วยเสมอเมื่อใช้ TypeORM
(กับดักเดิมของโปรเจกต์) · และบัญชีที่ถูกลบไปแล้วแต่ **เคยมี posting** ต้องยังปรากฏในงบของงวดที่มันมี
ยอด ไม่งั้นงบจะไม่ balance — ต้องใช้ `withDeleted()` + กรอง alias หลักเองตามที่ memory ของโปรเจกต์บันทึกไว้

### 3.3 เครื่องหมาย: flip ตาม `normal_balance` **เฉพาะตอนแสดงผล**

`TrialBalancesService` คืน `balance` เป็น "debit ลบ credit" ดิบ ๆ และ `gl_account_year_end_balances`
ก็เก็บแบบเดียวกัน — **งบการเงินต้องกลับเครื่องหมายให้บัญชีฝั่ง credit (LIABILITY/EQUITY/REVENUE)
ตอนพิมพ์เท่านั้น** ไม่ใช่ไปแก้ที่ ledger · ถ้าตัวเลขในงบกับงบทดลองเครื่องหมายไม่ตรงกัน คนอ่านจะสรุป
ว่าระบบใดระบบหนึ่งพัง

### 3.4 งบดุลต้องลงตัว และต้องบอกเมื่อไม่ลงตัว

`สินทรัพย์ = หนี้สิน + ส่วนของผู้ถือหุ้น + กำไรสะสมงวดปัจจุบัน` · ถ้าไม่เท่ากันให้ **แสดงส่วนต่างบนงบ
อย่างชัดเจน** (`is_balanced: false` + ยอดผลต่าง) ห้ามซ่อนหรือปัดให้ลงตัว — เหมือน `is_balanced` ของ
งบทดลองที่มีอยู่แล้ว · งบที่บาลานซ์เองโดยไม่มีใครรู้คือวิธีที่ผิดพลาดทางบัญชีหลุดออกไปถึงผู้สอบบัญชี

### 3.5 กำไรสะสมของงวดที่ยังไม่ปิดปี

ก่อนปิดปีบัญชี กำไรของงวดยังไม่ถูกโอนเข้า `RetainedEarnings` — งบดุลระหว่างปีจึงต้องบวก
**"กำไร(ขาดทุน)สุทธิระหว่างงวด"** ที่คำนวณสด (REVENUE − EXPENSE ของงวด) เข้าไปในส่วนของผู้ถือหุ้น
เป็นบรรทัดแยก ไม่ใช่ไปเขียนทับยอดบัญชีกำไรสะสม · หลังปิดปีแล้ว ยอดนั้นจะย้ายไปอยู่ใน
`RetainedEarnings` จริง และบรรทัดนี้จะเป็นศูนย์เอง — **ถ้าไม่ทำข้อนี้ งบดุลระหว่างปีจะไม่ balance เสมอ**

---

## 4 · API ที่เสนอ

| Endpoint | Permission | คืนอะไร |
|---|---|---|
| `GET /finance-bc/v1/balance-sheets?as_of=&compare_as_of=` | `balance_sheet:view` | ต้นไม้ ASSET/LIABILITY/EQUITY + ยอดต่อชั้น + บรรทัดกำไรระหว่างงวด + `is_balanced` |
| `GET /finance-bc/v1/income-statements?date_from=&date_to=&compare_*=` | `income_statement:view` | ต้นไม้ REVENUE/EXPENSE + กำไรขั้นต้น/สุทธิ |
| `POST /finance-bc/v1/balance-sheets/print` | `balance_sheet:print` | PDF (เฟส 2) |
| `POST /finance-bc/v1/income-statements/print` | `income_statement:print` | PDF (เฟส 2) |

**คนละ resource กัน ไม่ยุบเป็น `/financial-statements?type=`** — สองงบนี้ต่างกันที่แกนเวลา (งบดุล =
ณ วันที่ · งบกำไรขาดทุน = ช่วงเวลา) query param จึงคนละชุด และ endpoint เดียวที่รับทั้งสองแบบจะต้อง
validate แบบมีเงื่อนไข ซึ่งเป็นที่มาของบั๊กมากกว่าที่มันประหยัดโค้ด

**การพิมพ์ใช้แพตเทิร์นเดียวกับ ภ.พ.30 ทุกประการ** (ดู `HANDOFF-Backlog-Reporting-Print-Tax.md` §4.6):
ไม่มีแถวเอกสาร → `source_document_id` เป็น UUIDv5 ของงวด → พิมพ์ซ้ำงวดเดิมเดินเลขสำเนาต่อกัน และ
`document_prints.params` กลายเป็นบันทึกว่า "งบที่ส่งออกไปวันนั้นมีตัวเลขอะไร"

---

## 5 · ต้องตัดสินใจก่อนเริ่ม — 3 ข้อ

| # | คำถาม | ทางเลือก | ค่า default ถ้าไม่ตอบ |
|---|---|---|---|
| 1 | **ระดับความละเอียด** | (ก) ย่อ — เฉพาะบัญชีหัวข้อชั้นบน · (ข) เต็ม — ทุกบัญชีที่มียอด จัดชั้นตามผัง · (ค) ทั้งสองแบบผ่าน `?detail=` | **(ค)** — query เดียวกัน ต่างกันแค่ตัดชั้นตอนประกอบผลลัพธ์ ต้นทุนเพิ่มน้อยกว่าการเดาผิดแล้วทำใหม่ |
| 2 | **เทียบงวดก่อนหน้า** | งบการเงินไทยปกติพิมพ์ 2 ปีเทียบกัน · จะเทียบ "ปีก่อน" อัตโนมัติ หรือให้ผู้ใช้เลือกงวดเปรียบเทียบเอง | **ให้เลือกเอง** (`compare_as_of` / `compare_date_from|to`) และถ้าไม่ส่งมา = ไม่เทียบ — ระบบยังไม่เคยปิดปีสักปี การเทียบอัตโนมัติจะได้คอลัมน์ศูนย์ที่ดูเหมือนบั๊ก |
| 3 | **รอบแรกออกอะไร** | (ก) JSON อย่างเดียวเหมือน ภ.พ.30 v1 · (ข) JSON + ฟอร์มพิมพ์ในรอบเดียว | **(ข)** — print engine พร้อมแล้ว และงบที่พิมพ์ไม่ได้ก็เอาไปยื่น/ส่งผู้สอบไม่ได้ · แต่ต้องได้ **แบบฟอร์มงบที่ฝ่ายบัญชีใช้อยู่จริง** ก่อน ไม่งั้นชื่อบรรทัดจะเป็นการเดา |

⚠️ **ข้อ 3 คือข้อที่บล็อกจริง** — ชื่อบรรทัดบนงบ (เช่น "เงินสดและรายการเทียบเท่าเงินสด",
"ลูกหนี้การค้าและลูกหนี้อื่น") เป็นถ้อยคำที่ผู้สอบบัญชีคาดหวัง ไม่ใช่ชื่อบัญชีในผังตรง ๆ ·
ถ้าไม่มีแบบฟอร์มให้ ให้ทำ **(ก) JSON ก่อน** แล้วรอแบบฟอร์มค่อยทำการพิมพ์ ดีกว่าพิมพ์ด้วยชื่อที่เดาเอง

---

## 6 · แผนงานเป็นเฟส

| เฟส | ขอบเขต | ประมาณ |
|---|---|---|
| **P1** | `FinancialStatementsService` — query subtree + flip เครื่องหมาย + กำไรระหว่างงวด (§3.5) + unit test ครอบเคสบาลานซ์/ไม่บาลานซ์ | 1.5–2 วัน |
| **P2** | 2 endpoint + DTO + `@ValidatedQuery` + permission sync + grant migration (**2 deploy** ตามกับดัก permissions.md) | 1 วัน |
| **P3** | smoke — ยิงจริงแล้วเทียบยอดกับ `/trial-balances` ของช่วงเดียวกัน (ดู §7) | 0.5 วัน |
| **P4** | เทมเพลตพิมพ์ 2 ใบ + mapper + endpoint print (ขึ้นกับ §5 ข้อ 3) | 1.5–2 วัน |
| **P5** | หน้า Admin UI ใน `apps/iam` (ถ้าต้องการ — ดู §8) | 1.5 วัน |
| | **รวม P1–P4** | **4.5–5.5 วัน** |

---

## 7 · แผนเทส — ข้อจำกัดที่ต้องรู้ก่อน

**ข้อมูลจริงบนคลัสเตอร์ยังน้อยมาก** (§1.2): 133 ledger entries, REVENUE ยังไม่มีสักรายการ, ยังไม่เคย
ปิดปีบัญชี · แปลว่า:

- **unit test ต้องเป็นตัวหลัก** — ประกอบผังบัญชีจำลอง 3 ชั้นแล้วยืนยันว่า subtree รวมถูก, เครื่องหมาย
  flip ถูกทั้ง 5 `account_type`, และงบดุลบาลานซ์เมื่อบวกกำไรระหว่างงวด
- **smoke ยืนยันความสอดคล้อง ไม่ใช่ยืนยันตัวเลข** — เทียบ `SUM` ของงบดุล+งบกำไรขาดทุนกับ
  `/trial-balances` ช่วงเดียวกัน ถ้าตรงแปลว่าการจัดกลุ่มไม่ทำตัวเลขหาย · เป็นวิธีเดียวที่ได้ผลจริง
  บนข้อมูลที่ยังบางขนาดนี้ (แพตเทิร์นเดียวกับ smoke ของ ภ.พ.30 ที่เทียบ snapshot กับ endpoint)
- **อย่าสร้างเอกสารปลอมเพื่อทดสอบบน DB ที่ใช้ร่วมกับ production** (กับดัก #10) — ถ้าจำเป็นต้องมี
  REVENUE จริง ให้ออกใบเสร็จจริงแล้วยกเลิกคืนเหมือนที่รอบ ทวิ 50 ทำ และบันทึกไว้ว่าทำอะไรไป

---

## 8 · ขอบเขตที่ **ไม่** อยู่ในรอบนี้

- **งบกระแสเงินสด** — ต้องจำแนกทุกรายการเป็นกิจกรรมดำเนินงาน/ลงทุน/จัดหาเงิน ซึ่งเป็นข้อมูลที่
  `ledger_entries` วันนี้ไม่มี (ต้องเพิ่ม classification ที่ผังบัญชีหรือที่รายการ) — เป็นงานคนละก้อน
- **หมายเหตุประกอบงบการเงิน** — เป็นงานเอกสาร/ข้อความ ไม่ใช่งานคำนวณ
- **manual journal entry** — ยังไม่มีทั้งระบบ และเป็นตัวบล็อกของการปรับปรุงรายการปิดงบ
  (`HANDOFF-Fiscal-Year-Closing.md` §9) · งบการเงินใช้งานได้โดยไม่มีมัน แต่ผู้ใช้จะปรับปรุงรายการไม่ได้
- **Admin UI** — ทำได้ทีหลังโดยไม่ต้องแก้ backend เลย (P5 แยกไว้ด้วยเหตุผลนี้)

---

## 9 · Log การตัดสินใจ

| # | คำถาม | คำตอบ | เมื่อไหร่ |
|---|---|---|---|
| F1 | สร้างตารางสรุปของงบไหม | **ไม่** — งบคือมุมมองบน `ledger_entries` ทุกตัวเลขต้องสืบย้อนได้ (§3.1) | 2026-09-13 (ผู้เขียน handoff) |
| F2 | endpoint เดียวรับทั้งสองงบไหม | **ไม่** — คนละแกนเวลา คนละ query param (§4) | 2026-09-13 |
| F3 | flip เครื่องหมายที่ไหน | **ตอนแสดงผลเท่านั้น** ledger และงบทดลองยังเป็น debit−credit ดิบ (§3.3) | 2026-09-13 |
| F4 | งบไม่บาลานซ์ทำยังไง | **แสดงส่วนต่างให้เห็น** ห้ามปัดให้ลงตัว (§3.4) | 2026-09-13 |
| F5 | ระดับความละเอียด / เทียบงวด / พิมพ์เลยไหม | **ตอบแล้ว** — เต็ม (`?detail=`) · ไม่เทียบงวด · JSON ก่อน (§10.1) | 2026-09-19 (ผู้ใช้) |
| F6 | แยกต้นทุนขายออกจากค่าใช้จ่ายด้วยอะไร | **คอลัมน์ `gl_accounts.statement_section`** ไม่ใช่กฎจากรหัสบัญชี — ลูกค้าเปลี่ยนรหัสได้ (§10.2) | 2026-09-19 (ผู้ใช้อนุมัติข้อเสนอ) |
| F7 | flip เครื่องหมายตามอะไร | **`account_type`** ไม่ใช่ `normal_balance` — แก้จาก §3.3 เพราะบัญชีปรับมูลค่า (§10.2) | 2026-09-19 |

---

## 10 · ผลตรวจสอบ — 2026-09-19 ✅ **P1–P3 implement + verify + smoke (JSON เท่านั้น ยังไม่พิมพ์)**

`pnpm verify finance-bc` เขียวครบ 6 ขั้น · unit 14 เคสใหม่ (finance-bc รวม 434) · e2e 36/36 ·
smoke ไฟล์ใหม่ `apps/finance-bc/test/smoke/financial-statements.smoke.mjs` ผ่านบน DB จริง ·
migration รันบน DB จริงแล้วทั้ง 3 ตัว (2 ของ `erp_finance`, 1 grant ของ `erp_iam`) ·
`npm_config_name=Check npm run migration:generate:finance` = **No changes** (ไม่มี drift) ·
**deploy แล้ว** commit `80e1bd3` run `35453572794` — ยืนยันบนเครื่องจริงว่า route ทั้งสองตอบ 401 ผ่าน guard

### 10.1 · §5 ทั้งสามข้อ — ได้คำตอบจากงบจริงของฝ่ายบัญชี ไม่ใช่จากการเดา

ผู้ใช้ส่งไฟล์งบกำไรขาดทุนจริง (Excel, 4 ชั้น, ช่วง 01/01/2026–19/09/2026) มาให้ ซึ่งตอบ §5 ให้เองเกือบหมด:

| ข้อ | คำตอบ | มาจากไหนในไฟล์ |
|---|---|---|
| 1 · ระดับความละเอียด | **(ค) ทั้งสองแบบ** — `?detail=full` (ค่าเริ่มต้น) / `summary` | ไฟล์จริงเป็นแบบเต็มทุกชั้น จึงให้ full เป็น default |
| 2 · เทียบงวดก่อน | **ไม่เทียบ** ในรอบนี้ (ไม่มี `compare_*` param เลย) | ไฟล์จริงมีคอลัมน์เดียว ไม่มีคอลัมน์เทียบ |
| 3 · รอบแรกออกอะไร | **(ก) JSON อย่างเดียว** ตามที่ผู้ใช้สั่ง | — |

⚠️ **ตัวบล็อกเดิมของข้อ 3 หมดไปแล้ว แต่ไม่ใช่เหตุผลที่ยังไม่พิมพ์** — ที่เคยกลัวว่า "ชื่อบรรทัดบนงบจะเป็นการเดา"
ไม่เป็นความจริงอีกต่อไป: ไล่เทียบทุกบรรทัดในไฟล์จริงกับผังบัญชีที่ seed ไว้แล้ว **ตรงกันทุกบรรทัด**
(`รายได้จากการให้บริการ` 4100-02 · `ต้นทุนงานบริการ` 5210-00 · `ค่าจ้างทำโปรเจค` 5230-00 ·
`เงินสมทบกองทุนประกันสังคม` 5412-12 · `ค่าไปรษณีย์` 5430-04 · `ค่าธรรมเนียมธนาคาร` 5460-04 ·
`ค่าบริการทำบัญชี` 5470-01 · `เบี้ยปรับเงินเพิ่ม` 5490-02) — ผังมาตรฐานที่ seed ไว้ชัดเจนว่าถอดมาจากระบบบัญชีตัวเดียวกัน
**ชื่อบรรทัดบนงบจึงมาจาก `gl_accounts.name_th` ตรง ๆ ไม่ต้อง hard-code และไม่ต้องเดา** · P4 (ฟอร์มพิมพ์)
เหลือแค่งานเทมเพลต ไม่มีคำถามค้างอีก

**ที่ผังของเราต่างจากไฟล์จริง 1 จุด** — `ค่าบริการอื่น` ไฟล์จริงวางใต้ *ค่าใช้จ่ายอื่น ๆ*
แต่ผังที่ seed ไว้วาง `5420-09` ใต้ *ค่าใช้จ่ายสำนักงาน* · ยอดรวมไม่เปลี่ยน ย้ายเองได้จากหน้า `gl-accounts`
ถ้าอยากให้หน้าตาตรงกับของเดิมเป๊ะ

### 10.2 · สิ่งที่ของจริงออกมาต่างจากที่ร่างไว้ใน §3 (อ่านก่อนแก้โค้ดนี้)

**1. `flip เครื่องหมายตาม account_type` ไม่ใช่ `normal_balance`** — §3.3 เขียนไว้ว่า flip ตาม `normal_balance`
ซึ่ง **ผิด** และถ้าทำตามจะพังเงียบ: `4100-03 รับคืนสินค้า` เป็น REVENUE ที่ `normal_balance = DEBIT`
(บัญชีปรับมูลค่า ซึ่ง docblock ของ `NormalBalance` เตือนไว้เองว่ามีเยอะในผังไทย) flip ตาม `normal_balance`
จะพิมพ์รับคืนสินค้าเป็น**บวก** แล้ว**บวกเข้ายอดขาย** · flip ตาม `account_type` มันจะติดลบในหมวดรายได้
ซึ่งคือสิ่งที่ถูกต้อง มี unit test ล็อกเคสนี้ไว้แล้ว

**2. คอลัมน์ใหม่ `gl_accounts.statement_section`** (enum 4 ค่า, nullable) — §3.2 สมมติว่ารวม subtree
ได้ตรง ๆ แต่พอเอาผังจริงมาเทียบกับงบจริงก็เจอว่า **`account_type` บอกได้แค่ว่าบัญชีอยู่งบไหน ไม่ได้บอกว่าอยู่บรรทัดไหนของงบนั้น**:
ผังเก็บต้นทุนขาย (5100/5200) กับค่าใช้จ่าย (5300/5400/5500) ไว้ใต้ราก `5000-00 ค่าใช้จ่าย` เดียวกัน
แต่งบจริงแยกเป็นคนละก้อน — ถ้าไม่รู้ว่าก้อนไหนเป็นอะไรก็คำนวณ **กำไรขั้นต้น** ไม่ได้
- ตัดทิ้งไปแล้ว: กฎจากรหัสบัญชี (`51xx` = ต้นทุน) — `code` เป็น master data ที่ลูกค้าแก้ได้ตามดีไซน์
  ของ chart-of-accounts ทั้งชุด กฎแบบนี้จะเงียบ ๆ ผิดทันทีที่มีคนเปลี่ยนรหัส
- ใส่ที่**บัญชีคุมชั้นบน** ลูกสืบทอดจากบรรพบุรุษที่ใกล้ที่สุด — migration ติดป้าย **7 แถว** จัดกลุ่มบัญชี
  กำไรขาดทุนทั้ง 124 บัญชี
- **บัญชีที่ยังไม่ติดป้ายไม่หายไปจากงบ** ตกลงหมวดสำรองที่อยู่**ใต้**บรรทัดกำไรขั้นต้นเสมอ
  (REVENUE → `OTHER_INCOME`, EXPENSE → `OPERATING_EXPENSE`) บัญชีที่ลืมติดป้ายจึงทำให้กำไรขั้นต้น
  **ต่ำไป**ได้เท่านั้น ไม่มีทางสูงเกินจริง + แจ้งใน `meta.warnings` รหัส `UNTAGGED_STATEMENT_SECTION`

**3. ยอดของบัญชีคุมรวมเฉพาะลูกที่อยู่หมวดเดียวกัน** — amount ไต่ขึ้นจากบัญชีที่ถือยอดผ่านบรรพบุรุษที่
**เป็นเจ้าของหมวดนั้นจริง** (ติดป้ายเอง หรือสืบทอดมาจากที่ติดป้าย) เท่านั้น ไม่ใช่ "ไต่ขึ้นไปเรื่อย ๆ
ถ้าหมวดตรงกัน" — กติกาหลังทำให้ราก `5000-00` (ซึ่งตกหมวดสำรอง `OPERATING_EXPENSE`) กลืน subtree
ต้นทุนขายเข้ามาด้วย ยอดนับซ้ำข้ามหมวดทันที · เจอตอนเขียน unit test ไม่ใช่ตอน review

**4. ไม่มี `company` ในคำตอบ** — mockup ตอนแรกใส่หัวกระดาษ (ชื่อกิจการ/เลขผู้เสียภาษี) มาด้วย แต่ถอดออก:
print pipeline §2.2 กำหนดไว้แล้วว่า **report-bc เป็นคนดึง `company_profiles` มา merge เข้า params ตอนพิมพ์**
ใส่ตรงนี้ด้วยจะเป็นแหล่งที่สองของข้อมูลเดียวกัน และทำให้ `FinancialStatementModule` ต้อง import
`CompanyProfileModule` โดยไม่จำเป็น · FE ที่ต้องการหัวกระดาษบนจอเรียก `GET /company-profiles` เอง

**5. สร้างบน `TrialBalancesService.build()` ไม่ query `ledger_entries` เอง** — §3.2 ร่าง SQL ไว้ให้
แต่การใช้ query เดียวกับงบทดลองทำให้ §3.1 (“งบต้องตรงกับงบทดลองเสมอ”) เป็นจริง**เชิงโครงสร้าง**
ไม่ใช่แค่ตั้งใจ: ไม่มีทางเพี้ยนด้วยกฎปัดเศษ ขอบเขตวันที่ หรือ filter ที่ฝั่งใดฝั่งหนึ่งลืม เพราะมีชุดเดียว ·
แลกกับการรวม subtree ใน memory (217 บัญชี — ไม่มีนัยต่อ performance) และได้ผลพลอยได้คือบัญชีที่ถูกลบไปแล้ว
แต่เคยมี posting ยังโผล่ในงบ (งบทดลอง join ตารางแบบ raw ไม่ติด soft-delete filter) ซึ่ง §3.2 เตือนไว้เองว่าต้องระวัง

### 10.3 · สิ่งที่ smoke จับได้และ unit test จับไม่ได้

ตามข้อจำกัดใน §7 (REVENUE ยังไม่มีรายการจริงบนคลัสเตอร์) smoke **ไม่ยืนยันตัวเลข** แต่ยืนยัน
**ความสอดคล้อง** ซึ่งเป็นคุณสมบัติที่จริงไม่ว่ามีข้อมูลมากหรือน้อย:

- permission ทั้ง 2 ตัว sync **และ** grant แล้วจริง (ถ้าไม่ ก็ 403 ตั้งแต่ call แรก — กับดักลำดับ deploy)
- migration ทั้ง 2 ตัวรันบน DB นี้จริง (คอลัมน์มีอยู่ + 7 บัญชีคุมมีป้ายครบทั้ง 4 หมวด)
- **งบกระทบกับ `/trial-balances` ของช่วงเดียวกัน** ทั้ง 2 งบ (รายได้ · ค่าใช้จ่าย · สินทรัพย์ · หนี้สิน · ทุน)
- **ไม่มีบัญชีไหนหายไป** — ทุกบัญชีที่งบทดลองรายงานว่ามีรายการ ต้องโผล่ในงบที่ชั้นใดชั้นหนึ่ง
- `detail=summary` ให้ subtotal เท่ากับ `full` เป๊ะ (ต่างแค่รูปร่าง)
- `profit_for_period` ของงบดุล = `net_profit` ของงบกำไรขาดทุนช่วงปีบัญชีเดียวกัน (ถ้าไม่ตรง แปลว่าสองรายงาน
  จะขัดกันเองบนกระดาษแผ่นเดียวกัน)

ผลรันจริงรอบนี้: `assets 900, L+E 900, difference 0 (is_balanced=true)` · ฝั่ง P&L ยังว่าง
(มีบัญชี EXPENSE ที่มีรายการ 1 บัญชี ยอดสุทธิ 0) ตามที่ §1.2 บอกไว้ — **ตัวเลขจริงของงบกำไรขาดทุน
ยังพิสูจน์ไม่ได้บนคลัสเตอร์นี้จนกว่าจะมีรายได้จริงเข้าระบบ** ส่วนที่พิสูจน์ได้แล้วคือ unit test ซึ่งสร้าง
ผังจำลองแล้วยืนยันว่าได้ตัวเลขชุดเดียวกับไฟล์งบจริงของฝ่ายบัญชี **ทุกบรรทัด รวมถึงกำไรสุทธิ**

> ตัวเลขจริงของงบใบนั้นอยู่ในเทสต์ใน repo `erp-api` (private) เท่านั้น — ไฟล์นี้และตัวอย่าง JSON ใน
> `api-workflow-guide.html` ใช้**ตัวเลขสมมติ**ที่กระทบกันได้เอง เพราะ `plan-erp` เป็น public repo
> (ดูกติกาใน `CLAUDE.md` ของซับโมดูลนี้)

### 10.4 · ไฟล์ที่เพิ่ม/แก้

```
apps/finance-bc/src/modules/financial-statement/          ← ใหม่ทั้งโมดูล (14 ไฟล์)
  services/financial-statements.service.ts                ← หัวใจ: จัดกลุ่ม + flip + rollup + subtotal
  controllers/{income-statements,balance-sheets}.controller.ts
  dto/{income-statement,balance-sheet}-query-params.dto.ts
  enums/{statement-detail,balance-sheet-section}.enum.ts
  interfaces/ (6 ไฟล์)  constants/ (2 ไฟล์)  financial-statement.module.ts
apps/finance-bc/src/modules/general-ledger/
  enums/statement-section.enum.ts                         ← ใหม่ (+ STATEMENT_SECTIONS_BY_ACCOUNT_TYPE)
  entities/gl-account.entity.ts                           ← +statement_section
  services/gl-accounts.service.ts                         ← +findAllForStatement() +guard ของ statement_section
  dto/{create-gl-account,gl-account-response}.dto.ts      ← +statement_section
libs/database/src/migrations/erp_finance/
  1789785650931-AddStatementSectionToGlAccounts.ts        ← generate จาก entity diff
  1789785677293-TagStandardChartStatementSections.ts      ← DML ติดป้าย 7 บัญชีคุม (by id, guard IS NULL)
libs/database/src/migrations/erp_iam/
  1789785755432-GrantFinancialStatementPermissionsToMockPolicies.ts
apps/finance-bc/test/{unit/financial-statement,e2e,smoke}/ ← 14 unit + 8 e2e + 1 smoke
```

### 10.5 · เหลืออะไร

| งาน | สถานะ |
|---|---|
| **P4 · ฟอร์มพิมพ์ 2 ใบ** | ✅ **เสร็จ 2026-09-20** — ดู §11 |
| **P5 · Admin UI ใน `apps/iam`** | ✅ **เสร็จ 2026-09-20** — ดู §12 |
| ช่อง `statement_section` ในหน้า `gl-accounts` | ✅ **เสร็จ 2026-09-20** — อยู่ในฟอร์มสร้าง/แก้ไข เปลี่ยนตัวเลือกตามหมวดบัญชีอัตโนมัติ |
| เทียบงวดก่อนหน้า (`compare_*`) | ⬜ ตัดออกจากรอบนี้ตาม §10.1 ข้อ 2 |
| งบกระแสเงินสด · หมายเหตุประกอบงบ · manual journal entry | ⬜ นอกขอบเขตตาม §8 (ไม่เปลี่ยน) |

---

## 11 · ผลตรวจสอบ P4 — 2026-09-20 ✅ **พิมพ์งบได้จริงทั้งสองใบ**

`POST /finance-bc/v1/income-statements/print` และ `POST /finance-bc/v1/balance-sheets/print` ·
`pnpm verify finance-bc` เขียวครบ 6 ขั้น · unit 25 เคส (เพิ่ม 11 เคสของ mapper) · e2e 42/42 ·
smoke ไฟล์ใหม่ `financial-statements-print.smoke.mjs` ผ่านบน DB/broker/Gotenberg/storage ชุดจริง ·
ทั้ง repo **1845 tests / 139 suites**

### 11.1 · สิ่งที่ต้องรู้ถ้าจะทำแบบ ภ.ง.ด. หรือรายงานอื่นที่ไม่มีแถวเอกสารต่อ

- **งวดทำหน้าที่แทน id** — `source_document_id` = UUIDv5 ของงวด ใช้ namespace **คนละตัวกับ ภ.พ.30**
  (`FINANCIAL_STATEMENT_PERIOD_ID_NAMESPACE`) เพราะเป็นคนละสายเอกสาร **ห้ามเปลี่ยนค่า** จะรีเซ็ตประวัติสำเนาทั้งหมด
- **`detail` ไม่อยู่ใน seed ของ id** โดยตั้งใจ — พิมพ์ `full` แล้วพิมพ์ `summary` ของงวดเดียวกันคือ
  **สำเนา 2 ใบของงบใบเดียว** ไม่ใช่เอกสารคนละใบ · smoke ยืนยันข้อนี้ด้วยการพิมพ์สองครั้งแล้วเช็คว่า
  `source_document_id` เท่ากันและ `copy_number` เดินต่อ
- **mapper แผ่ต้นไม้เป็นแถวแบนให้เสร็จก่อนส่งไปพิมพ์** — print engine แทนที่ได้แค่ `{{row.field}}`
  ไม่มี conditional ไม่ recurse เข้า `children[]` และคำนวณ indent เองไม่ได้ · ทุกอย่างจึงมาเป็น**สตริง**:
  ระดับชั้น → CSS class ใน `row_class` (`sec`/`lv1`–`lv4`/`sub`/`tot`), ยอดที่ไม่ยืนยัน → `''` ไม่ใช่ `0.00`
- **บรรทัดผลต่างของงบดุลถูก "ไม่ใส่" ไม่ใช่ "ใส่แล้วซ่อน"** — engine ไม่มี conditional ดังนั้น
  "แสดงเฉพาะตอนที่สำคัญ" ต้องแปลว่า "ตอนที่ mapper ใส่มันลงใน `items[]`" · ตอนงบไม่ลงตัวจะได้ทั้งแถวผลต่าง
  **และ** ข้อความกำกับที่ชี้ให้ไปไล่ที่ ledger ไม่ใช่ที่งบ (smoke เช็คความสอดคล้องระหว่าง `is_balanced` กับข้อความนี้)
- **เทมเพลตไฟล์เดียวใช้สองงบ** (`financial-statement.html`) ตามหลัก "one file serves a family" ของ
  `assets/templates/_README.md` — ความต่างทั้งหมด (ชื่องบ, ป้ายงวด, ข้อความผลต่าง) มาเป็น params

### 11.2 · ลำดับงานที่ต้องทำซ้ำถ้า deploy ขึ้น environment ใหม่

เทมเพลตกับ document type **ไม่ได้อยู่ใน migration** — `print_templates.html_content` เก็บใน object storage
ซึ่งมีแต่ API ที่อัปโหลดให้ได้ (ดู `_README.md`) · บน cluster นี้สร้างผ่าน API แล้ว:

| code | print_template | document_type |
|---|---|---|
| `income_statement` | สร้างแล้ว (banded, A4, portrait) | สร้างแล้ว · `has_running_number = false` |
| `balance_sheet` | สร้างแล้ว (banded, A4, portrait) | สร้างแล้ว · `has_running_number = false` |

smoke ขั้นที่ 0 เช็คให้ว่าทั้งสองแถวมีอยู่จริงและเป็น engine `banded` — ถ้า environment ใหม่ลืมทำขั้นนี้
จะ fail ตรงนั้นพร้อมข้อความบอกวิธี ไม่ใช่ fail ลึก ๆ ตอน render

### 11.3 · หน้าตาของงบที่พิมพ์ออกมา

`รายได้ → ต้นทุนขาย → กำไรขั้นต้น → รายได้อื่น → ค่าใช้จ่าย → กำไร(ขาดทุน)สุทธิ` — กำไรขั้นต้นอยู่ใต้ต้นทุนขาย
ทันทีเพราะเป็นการลบที่อยู่เหนือมันพอดี · **ไฟล์งบของฝ่ายบัญชีไม่มีบรรทัดกำไรขั้นต้นเลย** (พิมพ์
รายได้/ต้นทุนขาย/ค่าใช้จ่าย/กำไรสุทธิ) — ที่เพิ่มมาเป็นการเติม ไม่ใช่การเปลี่ยน: ทุกบรรทัดที่ใบนั้นมี ยังอยู่ครบ
ลำดับเดิม แค่มี subtotal แทรกเพิ่ม 1 บรรทัด · ถ้าฝ่ายบัญชีอยากได้ตรงของเดิมเป๊ะ ลบ `subtotalRow('กำไรขั้นต้น', …)`
บรรทัดเดียวใน `financial-statement-print-params.util.ts`

งบดุลพิมพ์ `สินทรัพย์ → รวมสินทรัพย์ → หนี้สิน → รวมหนี้สิน → ส่วนของผู้ถือหุ้น → กำไร(ขาดทุน)ระหว่างงวด →
รวมส่วนของผู้ถือหุ้น → รวมหนี้สินและส่วนของผู้ถือหุ้น` (+ บรรทัดผลต่างเมื่อไม่ลงตัว)

---

## 12 · ผลตรวจสอบ P5 — 2026-09-20 ✅ **Admin UI (ยังไม่ผ่าน manual QA)**

`pnpm verify iam` เขียวครบ 6 ขั้น · smoke ไฟล์ใหม่ `apps/iam/test/smoke/financial-statements-page.smoke.mjs` ·
`admin-pages.smoke.mjs` นับหน้าเป็น **19 หน้า** แล้ว · repo รวม **1845 tests / 139 suites**

### 12.1 · สิ่งที่ทำ

- **หน้าใหม่ `/views/financial-statements`** — เลือกงบ → เลือกงวด → กด "แสดงงบ" → อ่านตารางที่ย่อหน้าตามชั้นผังบัญชี
  → กด "พิมพ์งบ" ได้จากแถบบนขวา · มีแถบสรุปยอด (กำไรขั้นต้น/กำไรสุทธิ ฯลฯ) อยู่เหนือตาราง
- **หน้าเดียวรับทั้งสองงบ** ไม่แยกสองหน้า — อ่านเหมือนกันทุกประการ ต่างกันแค่แกนเวลา ซึ่งเป็นสิ่งที่ dropdown
  ตัวแรกสลับ (และช่อง "ตั้งแต่วันที่" ซ่อนตัวเองเมื่อเลือกงบดุล เพราะงบดุลสะสมตั้งแต่รายการแรก ไม่มีขอบล่างให้เลือก) ·
  **ฝั่ง API ยังแยกเป็นสอง resource** ตามเหตุผลใน §4 — คนละเรื่องกับจำนวนเมนู
- **หน้านี้เป็นหน้าเดียวที่อ่าน `meta.warnings`** — จึงเรียก API ผ่าน `fetchWithAuth` ตรง ๆ แทน `financeGet`
  เพราะ JSON:API client กลางแกะเอาแต่ `data` แล้วทิ้ง envelope ที่เหลือ · warning ตัวที่สำคัญคือ
  `UNTAGGED_STATEMENT_SECTION`: ถ้าหายไป หน้าจะแสดงกำไรขั้นต้นที่**ต่ำกว่าความจริง**อย่างหน้าตาเฉย
  ซึ่งแย่กว่าฟีเจอร์หาย · smoke ล็อกข้อนี้ไว้
- **ช่อง `statement_section` ในฟอร์ม `gl-accounts`** — ตัวเลือกเปลี่ยนตามหมวดบัญชีที่เลือกอยู่
  (REVENUE → รายได้/รายได้อื่น · EXPENSE → ต้นทุนขาย/ค่าใช้จ่าย) และ**ปิดตัวเอง + ล้างค่า**สำหรับบัญชีงบดุล
  เพราะ server ปฏิเสธอยู่แล้ว การยื่นตัวเลือกให้เลือกก็ได้แค่ 400 ที่ผู้ใช้ต้องมาแปลเอง ·
  ตารางค่าที่ใช้ mirror `STATEMENT_SECTIONS_BY_ACCOUNT_TYPE` ฝั่ง server
- **CSS ของตารางงบอยู่ใน `table.css` (shared)** ไม่ใช่ CSS ต่อหน้า — ตามกติกาใน `.claude/rules/admin-ui-ejs.md`
  และทุกสีเป็น token เพราะ `admin-theme-tokens.spec.ts` บังคับว่า theme.css เท่านั้นที่ตัดสินธีม

### 12.2 · ⬜ ค้าง — manual QA (ต้องมีคนคลิกจริง)

session ที่ทำ P5 **ต่อ browser tool ไม่ได้** (เหมือนรอบ gl-accounts เมื่อ 2026-09-06 และ 2026-09-11)
สิ่งที่ smoke ทดแทนให้แล้ว: ทุก id ที่ EJS ประกาศถูก bundle อ้างจริง · ทุก `onclick="fn(...)"` มีตัวจริง
ลงทะเบียนเป็น global (ไม่งั้นคลิกแล้ว ReferenceError เงียบ ๆ) · คลาสย่อหน้าทุกระดับยังมีใน `table.css` ·
ปุ่มพิมพ์ยังมี `data-permission`

**ที่ยังต้องมีคนดูด้วยตา**: ตัวเลขเรียงตรงหลักไหม · ย่อหน้าอ่านออกจริงไหมตอนผังลึก 4 ชั้น ·
ปุ่มพิมพ์เปิดแท็บ PDF ได้จริงบนเบราว์เซอร์จริง · แถบ warning อ่านออกทั้งธีมสว่างและมืด

### 12.3 · เหลือทั้งหมดของ D3

ไม่มีเฟสไหนค้างแล้ว · งานที่ **จงใจไม่ทำ** ในรอบนี้ยังเหมือนเดิม (§8): เทียบงวดก่อนหน้า ·
งบกระแสเงินสด · หมายเหตุประกอบงบ · manual journal entry
